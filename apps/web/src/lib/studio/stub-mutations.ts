import type { StudioMutations } from "@/store/studio";

export const stubMutations: StudioMutations = {
	setMatchStart: async () => undefined,
	updateProbeMetadata: async () => undefined,
	updateTimeline: async () => undefined,
};
