{
  "name": "@ontology/vision",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn vision.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uv run uvicorn vision.main:app --host 0.0.0.0 --port 8000",
    "lint": "uv run ruff check src tests",
    "lint:fix": "uv run ruff check --fix src tests",
    "test": "uv run pytest"
  }
}
